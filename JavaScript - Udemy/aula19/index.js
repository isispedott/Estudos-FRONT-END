//string, number, undefined, null, boolean, symbol

const nome = 'Luiz' //string
const sobrenome = "Silva" //string
const apelido = `Lulu` //string
const numero =  10; //number
const numero2 = 10.54; //number
let nomeAluno; // undefined (pois não recebeu um caminho - não a inicializamos)
let sobrenomeAluno = null; //Nulo -> não apontapra local algum 
const aprovado = true; // Boolean = true or false; 

console.log(typeof nome);